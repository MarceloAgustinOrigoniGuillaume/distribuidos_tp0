  client{ID}:
    container_name: client{ID}
    image: client:latest
    entrypoint: /client
    environment:
      - CLI_ID={ID}
      - CLI_LOG_LEVEL={LOG_LEVEL}
    networks:
      - testing_net
    depends_on:
      - server
    volumes:
      -{CONFIG_FILE}:/usr/config.yaml
